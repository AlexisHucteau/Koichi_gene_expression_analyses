---
title: "R Notebook"
output: html_notebook
---

# Gene set enrichment

## OR vs R

```{r}
dotplot(Gene_set_diff_analysis[["R_vs_OR"]], showCategory=10, split=".sign") + facet_grid(.~.sign)
```


## OR vs R no RUNX1m

```{r}
# png("../Results/NR_vs_R_Geneset.png", width = 720, height = 720*9/16)
dotplot(Gene_set_diff_analysis[["R_vs_OR_without_RUNX1m"]], showCategory=10, split=".sign") + facet_grid(.~.sign)
# dev.off()
```



## NR vs R

```{r}
dotplot(Gene_set_diff_analysis[["NR_vs_R"]], showCategory=10, split=".sign") + facet_grid(.~.sign)
```

## NR vs R no RUNX1m

```{r}
dotplot(Gene_set_diff_analysis[["NR_vs_R_without_RUNX1m"]], showCategory=10, split=".sign") + facet_grid(.~.sign)
```

# MS Viper

## R vs OR

```{r}
plot(R_OR_msViper$mrs)
plot(R_OR_msViper_synergy)
```

## R vs OR no RUNX1

```{r}
plot(R_OR_without_RUNX1m_msViper$mrs)
plot(R_OR_without_RUNX1m_msViper_synergy)
```

## NR vs R

```{r}
plot(R_NR_msViper$mrs)
plot(R_NR_msViper_synergy)
```

## NR vs R no RUNX1m

```{r}
plot(R_NR_without_RUNX1m_msViper$mrs)
plot(R_NR_without_RUNX1m_msViper_synergy)
```

